// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"server/infrastructure/ent/predicate"
	"server/infrastructure/ent/topic"
	"server/infrastructure/ent/user"
	"server/infrastructure/ent/usertopicnotification"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// UserTopicNotificationQuery is the builder for querying UserTopicNotification entities.
type UserTopicNotificationQuery struct {
	config
	ctx        *QueryContext
	order      []usertopicnotification.OrderOption
	inters     []Interceptor
	predicates []predicate.UserTopicNotification
	withUser   *UserQuery
	withTopic  *TopicQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserTopicNotificationQuery builder.
func (utnq *UserTopicNotificationQuery) Where(ps ...predicate.UserTopicNotification) *UserTopicNotificationQuery {
	utnq.predicates = append(utnq.predicates, ps...)
	return utnq
}

// Limit the number of records to be returned by this query.
func (utnq *UserTopicNotificationQuery) Limit(limit int) *UserTopicNotificationQuery {
	utnq.ctx.Limit = &limit
	return utnq
}

// Offset to start from.
func (utnq *UserTopicNotificationQuery) Offset(offset int) *UserTopicNotificationQuery {
	utnq.ctx.Offset = &offset
	return utnq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (utnq *UserTopicNotificationQuery) Unique(unique bool) *UserTopicNotificationQuery {
	utnq.ctx.Unique = &unique
	return utnq
}

// Order specifies how the records should be ordered.
func (utnq *UserTopicNotificationQuery) Order(o ...usertopicnotification.OrderOption) *UserTopicNotificationQuery {
	utnq.order = append(utnq.order, o...)
	return utnq
}

// QueryUser chains the current query on the "user" edge.
func (utnq *UserTopicNotificationQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: utnq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := utnq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := utnq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(usertopicnotification.Table, usertopicnotification.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, usertopicnotification.UserTable, usertopicnotification.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(utnq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTopic chains the current query on the "topic" edge.
func (utnq *UserTopicNotificationQuery) QueryTopic() *TopicQuery {
	query := (&TopicClient{config: utnq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := utnq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := utnq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(usertopicnotification.Table, usertopicnotification.FieldID, selector),
			sqlgraph.To(topic.Table, topic.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, usertopicnotification.TopicTable, usertopicnotification.TopicColumn),
		)
		fromU = sqlgraph.SetNeighbors(utnq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first UserTopicNotification entity from the query.
// Returns a *NotFoundError when no UserTopicNotification was found.
func (utnq *UserTopicNotificationQuery) First(ctx context.Context) (*UserTopicNotification, error) {
	nodes, err := utnq.Limit(1).All(setContextOp(ctx, utnq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{usertopicnotification.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (utnq *UserTopicNotificationQuery) FirstX(ctx context.Context) *UserTopicNotification {
	node, err := utnq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserTopicNotification ID from the query.
// Returns a *NotFoundError when no UserTopicNotification ID was found.
func (utnq *UserTopicNotificationQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = utnq.Limit(1).IDs(setContextOp(ctx, utnq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{usertopicnotification.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (utnq *UserTopicNotificationQuery) FirstIDX(ctx context.Context) int {
	id, err := utnq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserTopicNotification entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserTopicNotification entity is found.
// Returns a *NotFoundError when no UserTopicNotification entities are found.
func (utnq *UserTopicNotificationQuery) Only(ctx context.Context) (*UserTopicNotification, error) {
	nodes, err := utnq.Limit(2).All(setContextOp(ctx, utnq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{usertopicnotification.Label}
	default:
		return nil, &NotSingularError{usertopicnotification.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (utnq *UserTopicNotificationQuery) OnlyX(ctx context.Context) *UserTopicNotification {
	node, err := utnq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserTopicNotification ID in the query.
// Returns a *NotSingularError when more than one UserTopicNotification ID is found.
// Returns a *NotFoundError when no entities are found.
func (utnq *UserTopicNotificationQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = utnq.Limit(2).IDs(setContextOp(ctx, utnq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{usertopicnotification.Label}
	default:
		err = &NotSingularError{usertopicnotification.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (utnq *UserTopicNotificationQuery) OnlyIDX(ctx context.Context) int {
	id, err := utnq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserTopicNotifications.
func (utnq *UserTopicNotificationQuery) All(ctx context.Context) ([]*UserTopicNotification, error) {
	ctx = setContextOp(ctx, utnq.ctx, "All")
	if err := utnq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UserTopicNotification, *UserTopicNotificationQuery]()
	return withInterceptors[[]*UserTopicNotification](ctx, utnq, qr, utnq.inters)
}

// AllX is like All, but panics if an error occurs.
func (utnq *UserTopicNotificationQuery) AllX(ctx context.Context) []*UserTopicNotification {
	nodes, err := utnq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserTopicNotification IDs.
func (utnq *UserTopicNotificationQuery) IDs(ctx context.Context) (ids []int, err error) {
	if utnq.ctx.Unique == nil && utnq.path != nil {
		utnq.Unique(true)
	}
	ctx = setContextOp(ctx, utnq.ctx, "IDs")
	if err = utnq.Select(usertopicnotification.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (utnq *UserTopicNotificationQuery) IDsX(ctx context.Context) []int {
	ids, err := utnq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (utnq *UserTopicNotificationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, utnq.ctx, "Count")
	if err := utnq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, utnq, querierCount[*UserTopicNotificationQuery](), utnq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (utnq *UserTopicNotificationQuery) CountX(ctx context.Context) int {
	count, err := utnq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (utnq *UserTopicNotificationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, utnq.ctx, "Exist")
	switch _, err := utnq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (utnq *UserTopicNotificationQuery) ExistX(ctx context.Context) bool {
	exist, err := utnq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserTopicNotificationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (utnq *UserTopicNotificationQuery) Clone() *UserTopicNotificationQuery {
	if utnq == nil {
		return nil
	}
	return &UserTopicNotificationQuery{
		config:     utnq.config,
		ctx:        utnq.ctx.Clone(),
		order:      append([]usertopicnotification.OrderOption{}, utnq.order...),
		inters:     append([]Interceptor{}, utnq.inters...),
		predicates: append([]predicate.UserTopicNotification{}, utnq.predicates...),
		withUser:   utnq.withUser.Clone(),
		withTopic:  utnq.withTopic.Clone(),
		// clone intermediate query.
		sql:  utnq.sql.Clone(),
		path: utnq.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (utnq *UserTopicNotificationQuery) WithUser(opts ...func(*UserQuery)) *UserTopicNotificationQuery {
	query := (&UserClient{config: utnq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	utnq.withUser = query
	return utnq
}

// WithTopic tells the query-builder to eager-load the nodes that are connected to
// the "topic" edge. The optional arguments are used to configure the query builder of the edge.
func (utnq *UserTopicNotificationQuery) WithTopic(opts ...func(*TopicQuery)) *UserTopicNotificationQuery {
	query := (&TopicClient{config: utnq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	utnq.withTopic = query
	return utnq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID int `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserTopicNotification.Query().
//		GroupBy(usertopicnotification.FieldUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (utnq *UserTopicNotificationQuery) GroupBy(field string, fields ...string) *UserTopicNotificationGroupBy {
	utnq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UserTopicNotificationGroupBy{build: utnq}
	grbuild.flds = &utnq.ctx.Fields
	grbuild.label = usertopicnotification.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID int `json:"user_id,omitempty"`
//	}
//
//	client.UserTopicNotification.Query().
//		Select(usertopicnotification.FieldUserID).
//		Scan(ctx, &v)
func (utnq *UserTopicNotificationQuery) Select(fields ...string) *UserTopicNotificationSelect {
	utnq.ctx.Fields = append(utnq.ctx.Fields, fields...)
	sbuild := &UserTopicNotificationSelect{UserTopicNotificationQuery: utnq}
	sbuild.label = usertopicnotification.Label
	sbuild.flds, sbuild.scan = &utnq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UserTopicNotificationSelect configured with the given aggregations.
func (utnq *UserTopicNotificationQuery) Aggregate(fns ...AggregateFunc) *UserTopicNotificationSelect {
	return utnq.Select().Aggregate(fns...)
}

func (utnq *UserTopicNotificationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range utnq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, utnq); err != nil {
				return err
			}
		}
	}
	for _, f := range utnq.ctx.Fields {
		if !usertopicnotification.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if utnq.path != nil {
		prev, err := utnq.path(ctx)
		if err != nil {
			return err
		}
		utnq.sql = prev
	}
	return nil
}

func (utnq *UserTopicNotificationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserTopicNotification, error) {
	var (
		nodes       = []*UserTopicNotification{}
		_spec       = utnq.querySpec()
		loadedTypes = [2]bool{
			utnq.withUser != nil,
			utnq.withTopic != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UserTopicNotification).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UserTopicNotification{config: utnq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, utnq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := utnq.withUser; query != nil {
		if err := utnq.loadUser(ctx, query, nodes, nil,
			func(n *UserTopicNotification, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	if query := utnq.withTopic; query != nil {
		if err := utnq.loadTopic(ctx, query, nodes, nil,
			func(n *UserTopicNotification, e *Topic) { n.Edges.Topic = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (utnq *UserTopicNotificationQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*UserTopicNotification, init func(*UserTopicNotification), assign func(*UserTopicNotification, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*UserTopicNotification)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (utnq *UserTopicNotificationQuery) loadTopic(ctx context.Context, query *TopicQuery, nodes []*UserTopicNotification, init func(*UserTopicNotification), assign func(*UserTopicNotification, *Topic)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*UserTopicNotification)
	for i := range nodes {
		fk := nodes[i].TopicID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(topic.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "topic_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (utnq *UserTopicNotificationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := utnq.querySpec()
	_spec.Node.Columns = utnq.ctx.Fields
	if len(utnq.ctx.Fields) > 0 {
		_spec.Unique = utnq.ctx.Unique != nil && *utnq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, utnq.driver, _spec)
}

func (utnq *UserTopicNotificationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(usertopicnotification.Table, usertopicnotification.Columns, sqlgraph.NewFieldSpec(usertopicnotification.FieldID, field.TypeInt))
	_spec.From = utnq.sql
	if unique := utnq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if utnq.path != nil {
		_spec.Unique = true
	}
	if fields := utnq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, usertopicnotification.FieldID)
		for i := range fields {
			if fields[i] != usertopicnotification.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if utnq.withUser != nil {
			_spec.Node.AddColumnOnce(usertopicnotification.FieldUserID)
		}
		if utnq.withTopic != nil {
			_spec.Node.AddColumnOnce(usertopicnotification.FieldTopicID)
		}
	}
	if ps := utnq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := utnq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := utnq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := utnq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (utnq *UserTopicNotificationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(utnq.driver.Dialect())
	t1 := builder.Table(usertopicnotification.Table)
	columns := utnq.ctx.Fields
	if len(columns) == 0 {
		columns = usertopicnotification.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if utnq.sql != nil {
		selector = utnq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if utnq.ctx.Unique != nil && *utnq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range utnq.predicates {
		p(selector)
	}
	for _, p := range utnq.order {
		p(selector)
	}
	if offset := utnq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := utnq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UserTopicNotificationGroupBy is the group-by builder for UserTopicNotification entities.
type UserTopicNotificationGroupBy struct {
	selector
	build *UserTopicNotificationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (utngb *UserTopicNotificationGroupBy) Aggregate(fns ...AggregateFunc) *UserTopicNotificationGroupBy {
	utngb.fns = append(utngb.fns, fns...)
	return utngb
}

// Scan applies the selector query and scans the result into the given value.
func (utngb *UserTopicNotificationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, utngb.build.ctx, "GroupBy")
	if err := utngb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserTopicNotificationQuery, *UserTopicNotificationGroupBy](ctx, utngb.build, utngb, utngb.build.inters, v)
}

func (utngb *UserTopicNotificationGroupBy) sqlScan(ctx context.Context, root *UserTopicNotificationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(utngb.fns))
	for _, fn := range utngb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*utngb.flds)+len(utngb.fns))
		for _, f := range *utngb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*utngb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := utngb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UserTopicNotificationSelect is the builder for selecting fields of UserTopicNotification entities.
type UserTopicNotificationSelect struct {
	*UserTopicNotificationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (utns *UserTopicNotificationSelect) Aggregate(fns ...AggregateFunc) *UserTopicNotificationSelect {
	utns.fns = append(utns.fns, fns...)
	return utns
}

// Scan applies the selector query and scans the result into the given value.
func (utns *UserTopicNotificationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, utns.ctx, "Select")
	if err := utns.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserTopicNotificationQuery, *UserTopicNotificationSelect](ctx, utns.UserTopicNotificationQuery, utns, utns.inters, v)
}

func (utns *UserTopicNotificationSelect) sqlScan(ctx context.Context, root *UserTopicNotificationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(utns.fns))
	for _, fn := range utns.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*utns.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := utns.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
