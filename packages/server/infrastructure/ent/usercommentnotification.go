// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"server/infrastructure/ent/comment"
	"server/infrastructure/ent/user"
	"server/infrastructure/ent/usercommentnotification"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// UserCommentNotification is the model entity for the UserCommentNotification schema.
type UserCommentNotification struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID int `json:"user_id,omitempty"`
	// CommentID holds the value of the "comment_id" field.
	CommentID int `json:"comment_id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the UserCommentNotificationQuery when eager-loading is set.
	Edges        UserCommentNotificationEdges `json:"edges"`
	selectValues sql.SelectValues
}

// UserCommentNotificationEdges holds the relations/edges for other nodes in the graph.
type UserCommentNotificationEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// Comment holds the value of the comment edge.
	Comment *Comment `json:"comment,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserCommentNotificationEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// CommentOrErr returns the Comment value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserCommentNotificationEdges) CommentOrErr() (*Comment, error) {
	if e.Comment != nil {
		return e.Comment, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: comment.Label}
	}
	return nil, &NotLoadedError{edge: "comment"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UserCommentNotification) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case usercommentnotification.FieldID, usercommentnotification.FieldUserID, usercommentnotification.FieldCommentID:
			values[i] = new(sql.NullInt64)
		case usercommentnotification.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UserCommentNotification fields.
func (ucn *UserCommentNotification) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case usercommentnotification.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ucn.ID = int(value.Int64)
		case usercommentnotification.FieldUserID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				ucn.UserID = int(value.Int64)
			}
		case usercommentnotification.FieldCommentID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field comment_id", values[i])
			} else if value.Valid {
				ucn.CommentID = int(value.Int64)
			}
		case usercommentnotification.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				ucn.CreatedAt = value.Time
			}
		default:
			ucn.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UserCommentNotification.
// This includes values selected through modifiers, order, etc.
func (ucn *UserCommentNotification) Value(name string) (ent.Value, error) {
	return ucn.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the UserCommentNotification entity.
func (ucn *UserCommentNotification) QueryUser() *UserQuery {
	return NewUserCommentNotificationClient(ucn.config).QueryUser(ucn)
}

// QueryComment queries the "comment" edge of the UserCommentNotification entity.
func (ucn *UserCommentNotification) QueryComment() *CommentQuery {
	return NewUserCommentNotificationClient(ucn.config).QueryComment(ucn)
}

// Update returns a builder for updating this UserCommentNotification.
// Note that you need to call UserCommentNotification.Unwrap() before calling this method if this UserCommentNotification
// was returned from a transaction, and the transaction was committed or rolled back.
func (ucn *UserCommentNotification) Update() *UserCommentNotificationUpdateOne {
	return NewUserCommentNotificationClient(ucn.config).UpdateOne(ucn)
}

// Unwrap unwraps the UserCommentNotification entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ucn *UserCommentNotification) Unwrap() *UserCommentNotification {
	_tx, ok := ucn.config.driver.(*txDriver)
	if !ok {
		panic("ent: UserCommentNotification is not a transactional entity")
	}
	ucn.config.driver = _tx.drv
	return ucn
}

// String implements the fmt.Stringer.
func (ucn *UserCommentNotification) String() string {
	var builder strings.Builder
	builder.WriteString("UserCommentNotification(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ucn.ID))
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", ucn.UserID))
	builder.WriteString(", ")
	builder.WriteString("comment_id=")
	builder.WriteString(fmt.Sprintf("%v", ucn.CommentID))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(ucn.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// UserCommentNotifications is a parsable slice of UserCommentNotification.
type UserCommentNotifications []*UserCommentNotification
