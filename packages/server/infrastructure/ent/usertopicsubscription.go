// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"server/infrastructure/ent/topic"
	"server/infrastructure/ent/user"
	"server/infrastructure/ent/usertopicsubscription"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// UserTopicSubscription is the model entity for the UserTopicSubscription schema.
type UserTopicSubscription struct {
	config `json:"-"`
	// UserId holds the value of the "userId" field.
	UserId int `json:"userId,omitempty"`
	// TopicId holds the value of the "topicId" field.
	TopicId int `json:"topicId,omitempty"`
	// 通知を受け取るかどうかのカラムを追加
	IsNotified bool `json:"isNotified,omitempty"`
	// SubscribedAt holds the value of the "subscribedAt" field.
	SubscribedAt time.Time `json:"subscribedAt,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the UserTopicSubscriptionQuery when eager-loading is set.
	Edges        UserTopicSubscriptionEdges `json:"edges"`
	selectValues sql.SelectValues
}

// UserTopicSubscriptionEdges holds the relations/edges for other nodes in the graph.
type UserTopicSubscriptionEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// Topic holds the value of the topic edge.
	Topic *Topic `json:"topic,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserTopicSubscriptionEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// TopicOrErr returns the Topic value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserTopicSubscriptionEdges) TopicOrErr() (*Topic, error) {
	if e.Topic != nil {
		return e.Topic, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: topic.Label}
	}
	return nil, &NotLoadedError{edge: "topic"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UserTopicSubscription) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case usertopicsubscription.FieldIsNotified:
			values[i] = new(sql.NullBool)
		case usertopicsubscription.FieldUserId, usertopicsubscription.FieldTopicId:
			values[i] = new(sql.NullInt64)
		case usertopicsubscription.FieldSubscribedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UserTopicSubscription fields.
func (uts *UserTopicSubscription) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case usertopicsubscription.FieldUserId:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field userId", values[i])
			} else if value.Valid {
				uts.UserId = int(value.Int64)
			}
		case usertopicsubscription.FieldTopicId:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field topicId", values[i])
			} else if value.Valid {
				uts.TopicId = int(value.Int64)
			}
		case usertopicsubscription.FieldIsNotified:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field isNotified", values[i])
			} else if value.Valid {
				uts.IsNotified = value.Bool
			}
		case usertopicsubscription.FieldSubscribedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field subscribedAt", values[i])
			} else if value.Valid {
				uts.SubscribedAt = value.Time
			}
		default:
			uts.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UserTopicSubscription.
// This includes values selected through modifiers, order, etc.
func (uts *UserTopicSubscription) Value(name string) (ent.Value, error) {
	return uts.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the UserTopicSubscription entity.
func (uts *UserTopicSubscription) QueryUser() *UserQuery {
	return NewUserTopicSubscriptionClient(uts.config).QueryUser(uts)
}

// QueryTopic queries the "topic" edge of the UserTopicSubscription entity.
func (uts *UserTopicSubscription) QueryTopic() *TopicQuery {
	return NewUserTopicSubscriptionClient(uts.config).QueryTopic(uts)
}

// Update returns a builder for updating this UserTopicSubscription.
// Note that you need to call UserTopicSubscription.Unwrap() before calling this method if this UserTopicSubscription
// was returned from a transaction, and the transaction was committed or rolled back.
func (uts *UserTopicSubscription) Update() *UserTopicSubscriptionUpdateOne {
	return NewUserTopicSubscriptionClient(uts.config).UpdateOne(uts)
}

// Unwrap unwraps the UserTopicSubscription entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (uts *UserTopicSubscription) Unwrap() *UserTopicSubscription {
	_tx, ok := uts.config.driver.(*txDriver)
	if !ok {
		panic("ent: UserTopicSubscription is not a transactional entity")
	}
	uts.config.driver = _tx.drv
	return uts
}

// String implements the fmt.Stringer.
func (uts *UserTopicSubscription) String() string {
	var builder strings.Builder
	builder.WriteString("UserTopicSubscription(")
	builder.WriteString("userId=")
	builder.WriteString(fmt.Sprintf("%v", uts.UserId))
	builder.WriteString(", ")
	builder.WriteString("topicId=")
	builder.WriteString(fmt.Sprintf("%v", uts.TopicId))
	builder.WriteString(", ")
	builder.WriteString("isNotified=")
	builder.WriteString(fmt.Sprintf("%v", uts.IsNotified))
	builder.WriteString(", ")
	builder.WriteString("subscribedAt=")
	builder.WriteString(uts.SubscribedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// UserTopicSubscriptions is a parsable slice of UserTopicSubscription.
type UserTopicSubscriptions []*UserTopicSubscription
