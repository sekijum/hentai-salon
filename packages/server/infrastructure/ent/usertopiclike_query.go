// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"server/infrastructure/ent/predicate"
	"server/infrastructure/ent/topic"
	"server/infrastructure/ent/user"
	"server/infrastructure/ent/usertopiclike"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
)

// UserTopicLikeQuery is the builder for querying UserTopicLike entities.
type UserTopicLikeQuery struct {
	config
	ctx        *QueryContext
	order      []usertopiclike.OrderOption
	inters     []Interceptor
	predicates []predicate.UserTopicLike
	withUser   *UserQuery
	withTopic  *TopicQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserTopicLikeQuery builder.
func (utlq *UserTopicLikeQuery) Where(ps ...predicate.UserTopicLike) *UserTopicLikeQuery {
	utlq.predicates = append(utlq.predicates, ps...)
	return utlq
}

// Limit the number of records to be returned by this query.
func (utlq *UserTopicLikeQuery) Limit(limit int) *UserTopicLikeQuery {
	utlq.ctx.Limit = &limit
	return utlq
}

// Offset to start from.
func (utlq *UserTopicLikeQuery) Offset(offset int) *UserTopicLikeQuery {
	utlq.ctx.Offset = &offset
	return utlq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (utlq *UserTopicLikeQuery) Unique(unique bool) *UserTopicLikeQuery {
	utlq.ctx.Unique = &unique
	return utlq
}

// Order specifies how the records should be ordered.
func (utlq *UserTopicLikeQuery) Order(o ...usertopiclike.OrderOption) *UserTopicLikeQuery {
	utlq.order = append(utlq.order, o...)
	return utlq
}

// QueryUser chains the current query on the "user" edge.
func (utlq *UserTopicLikeQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: utlq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := utlq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := utlq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(usertopiclike.Table, usertopiclike.UserColumn, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, usertopiclike.UserTable, usertopiclike.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(utlq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTopic chains the current query on the "topic" edge.
func (utlq *UserTopicLikeQuery) QueryTopic() *TopicQuery {
	query := (&TopicClient{config: utlq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := utlq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := utlq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(usertopiclike.Table, usertopiclike.TopicColumn, selector),
			sqlgraph.To(topic.Table, topic.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, usertopiclike.TopicTable, usertopiclike.TopicColumn),
		)
		fromU = sqlgraph.SetNeighbors(utlq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first UserTopicLike entity from the query.
// Returns a *NotFoundError when no UserTopicLike was found.
func (utlq *UserTopicLikeQuery) First(ctx context.Context) (*UserTopicLike, error) {
	nodes, err := utlq.Limit(1).All(setContextOp(ctx, utlq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{usertopiclike.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (utlq *UserTopicLikeQuery) FirstX(ctx context.Context) *UserTopicLike {
	node, err := utlq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// Only returns a single UserTopicLike entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserTopicLike entity is found.
// Returns a *NotFoundError when no UserTopicLike entities are found.
func (utlq *UserTopicLikeQuery) Only(ctx context.Context) (*UserTopicLike, error) {
	nodes, err := utlq.Limit(2).All(setContextOp(ctx, utlq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{usertopiclike.Label}
	default:
		return nil, &NotSingularError{usertopiclike.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (utlq *UserTopicLikeQuery) OnlyX(ctx context.Context) *UserTopicLike {
	node, err := utlq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// All executes the query and returns a list of UserTopicLikes.
func (utlq *UserTopicLikeQuery) All(ctx context.Context) ([]*UserTopicLike, error) {
	ctx = setContextOp(ctx, utlq.ctx, "All")
	if err := utlq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UserTopicLike, *UserTopicLikeQuery]()
	return withInterceptors[[]*UserTopicLike](ctx, utlq, qr, utlq.inters)
}

// AllX is like All, but panics if an error occurs.
func (utlq *UserTopicLikeQuery) AllX(ctx context.Context) []*UserTopicLike {
	nodes, err := utlq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// Count returns the count of the given query.
func (utlq *UserTopicLikeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, utlq.ctx, "Count")
	if err := utlq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, utlq, querierCount[*UserTopicLikeQuery](), utlq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (utlq *UserTopicLikeQuery) CountX(ctx context.Context) int {
	count, err := utlq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (utlq *UserTopicLikeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, utlq.ctx, "Exist")
	switch _, err := utlq.First(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (utlq *UserTopicLikeQuery) ExistX(ctx context.Context) bool {
	exist, err := utlq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserTopicLikeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (utlq *UserTopicLikeQuery) Clone() *UserTopicLikeQuery {
	if utlq == nil {
		return nil
	}
	return &UserTopicLikeQuery{
		config:     utlq.config,
		ctx:        utlq.ctx.Clone(),
		order:      append([]usertopiclike.OrderOption{}, utlq.order...),
		inters:     append([]Interceptor{}, utlq.inters...),
		predicates: append([]predicate.UserTopicLike{}, utlq.predicates...),
		withUser:   utlq.withUser.Clone(),
		withTopic:  utlq.withTopic.Clone(),
		// clone intermediate query.
		sql:  utlq.sql.Clone(),
		path: utlq.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (utlq *UserTopicLikeQuery) WithUser(opts ...func(*UserQuery)) *UserTopicLikeQuery {
	query := (&UserClient{config: utlq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	utlq.withUser = query
	return utlq
}

// WithTopic tells the query-builder to eager-load the nodes that are connected to
// the "topic" edge. The optional arguments are used to configure the query builder of the edge.
func (utlq *UserTopicLikeQuery) WithTopic(opts ...func(*TopicQuery)) *UserTopicLikeQuery {
	query := (&TopicClient{config: utlq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	utlq.withTopic = query
	return utlq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserId int `json:"userId,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserTopicLike.Query().
//		GroupBy(usertopiclike.FieldUserId).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (utlq *UserTopicLikeQuery) GroupBy(field string, fields ...string) *UserTopicLikeGroupBy {
	utlq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UserTopicLikeGroupBy{build: utlq}
	grbuild.flds = &utlq.ctx.Fields
	grbuild.label = usertopiclike.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserId int `json:"userId,omitempty"`
//	}
//
//	client.UserTopicLike.Query().
//		Select(usertopiclike.FieldUserId).
//		Scan(ctx, &v)
func (utlq *UserTopicLikeQuery) Select(fields ...string) *UserTopicLikeSelect {
	utlq.ctx.Fields = append(utlq.ctx.Fields, fields...)
	sbuild := &UserTopicLikeSelect{UserTopicLikeQuery: utlq}
	sbuild.label = usertopiclike.Label
	sbuild.flds, sbuild.scan = &utlq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UserTopicLikeSelect configured with the given aggregations.
func (utlq *UserTopicLikeQuery) Aggregate(fns ...AggregateFunc) *UserTopicLikeSelect {
	return utlq.Select().Aggregate(fns...)
}

func (utlq *UserTopicLikeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range utlq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, utlq); err != nil {
				return err
			}
		}
	}
	for _, f := range utlq.ctx.Fields {
		if !usertopiclike.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if utlq.path != nil {
		prev, err := utlq.path(ctx)
		if err != nil {
			return err
		}
		utlq.sql = prev
	}
	return nil
}

func (utlq *UserTopicLikeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserTopicLike, error) {
	var (
		nodes       = []*UserTopicLike{}
		_spec       = utlq.querySpec()
		loadedTypes = [2]bool{
			utlq.withUser != nil,
			utlq.withTopic != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UserTopicLike).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UserTopicLike{config: utlq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, utlq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := utlq.withUser; query != nil {
		if err := utlq.loadUser(ctx, query, nodes, nil,
			func(n *UserTopicLike, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	if query := utlq.withTopic; query != nil {
		if err := utlq.loadTopic(ctx, query, nodes, nil,
			func(n *UserTopicLike, e *Topic) { n.Edges.Topic = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (utlq *UserTopicLikeQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*UserTopicLike, init func(*UserTopicLike), assign func(*UserTopicLike, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*UserTopicLike)
	for i := range nodes {
		fk := nodes[i].UserId
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "userId" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (utlq *UserTopicLikeQuery) loadTopic(ctx context.Context, query *TopicQuery, nodes []*UserTopicLike, init func(*UserTopicLike), assign func(*UserTopicLike, *Topic)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*UserTopicLike)
	for i := range nodes {
		fk := nodes[i].TopicId
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(topic.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "topicId" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (utlq *UserTopicLikeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := utlq.querySpec()
	_spec.Unique = false
	_spec.Node.Columns = nil
	return sqlgraph.CountNodes(ctx, utlq.driver, _spec)
}

func (utlq *UserTopicLikeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(usertopiclike.Table, usertopiclike.Columns, nil)
	_spec.From = utlq.sql
	if unique := utlq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if utlq.path != nil {
		_spec.Unique = true
	}
	if fields := utlq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		for i := range fields {
			_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
		}
		if utlq.withUser != nil {
			_spec.Node.AddColumnOnce(usertopiclike.FieldUserId)
		}
		if utlq.withTopic != nil {
			_spec.Node.AddColumnOnce(usertopiclike.FieldTopicId)
		}
	}
	if ps := utlq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := utlq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := utlq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := utlq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (utlq *UserTopicLikeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(utlq.driver.Dialect())
	t1 := builder.Table(usertopiclike.Table)
	columns := utlq.ctx.Fields
	if len(columns) == 0 {
		columns = usertopiclike.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if utlq.sql != nil {
		selector = utlq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if utlq.ctx.Unique != nil && *utlq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range utlq.predicates {
		p(selector)
	}
	for _, p := range utlq.order {
		p(selector)
	}
	if offset := utlq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := utlq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UserTopicLikeGroupBy is the group-by builder for UserTopicLike entities.
type UserTopicLikeGroupBy struct {
	selector
	build *UserTopicLikeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (utlgb *UserTopicLikeGroupBy) Aggregate(fns ...AggregateFunc) *UserTopicLikeGroupBy {
	utlgb.fns = append(utlgb.fns, fns...)
	return utlgb
}

// Scan applies the selector query and scans the result into the given value.
func (utlgb *UserTopicLikeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, utlgb.build.ctx, "GroupBy")
	if err := utlgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserTopicLikeQuery, *UserTopicLikeGroupBy](ctx, utlgb.build, utlgb, utlgb.build.inters, v)
}

func (utlgb *UserTopicLikeGroupBy) sqlScan(ctx context.Context, root *UserTopicLikeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(utlgb.fns))
	for _, fn := range utlgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*utlgb.flds)+len(utlgb.fns))
		for _, f := range *utlgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*utlgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := utlgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UserTopicLikeSelect is the builder for selecting fields of UserTopicLike entities.
type UserTopicLikeSelect struct {
	*UserTopicLikeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (utls *UserTopicLikeSelect) Aggregate(fns ...AggregateFunc) *UserTopicLikeSelect {
	utls.fns = append(utls.fns, fns...)
	return utls
}

// Scan applies the selector query and scans the result into the given value.
func (utls *UserTopicLikeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, utls.ctx, "Select")
	if err := utls.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserTopicLikeQuery, *UserTopicLikeSelect](ctx, utls.UserTopicLikeQuery, utls, utls.inters, v)
}

func (utls *UserTopicLikeSelect) sqlScan(ctx context.Context, root *UserTopicLikeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(utls.fns))
	for _, fn := range utls.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*utls.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := utls.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
