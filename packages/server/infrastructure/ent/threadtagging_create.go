// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"server/infrastructure/ent/thread"
	"server/infrastructure/ent/threadtag"
	"server/infrastructure/ent/threadtagging"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ThreadTaggingCreate is the builder for creating a ThreadTagging entity.
type ThreadTaggingCreate struct {
	config
	mutation *ThreadTaggingMutation
	hooks    []Hook
}

// SetThreadId sets the "threadId" field.
func (ttc *ThreadTaggingCreate) SetThreadId(i int) *ThreadTaggingCreate {
	ttc.mutation.SetThreadId(i)
	return ttc
}

// SetTagId sets the "tagId" field.
func (ttc *ThreadTaggingCreate) SetTagId(i int) *ThreadTaggingCreate {
	ttc.mutation.SetTagId(i)
	return ttc
}

// SetThreadID sets the "thread" edge to the Thread entity by ID.
func (ttc *ThreadTaggingCreate) SetThreadID(id int) *ThreadTaggingCreate {
	ttc.mutation.SetThreadID(id)
	return ttc
}

// SetThread sets the "thread" edge to the Thread entity.
func (ttc *ThreadTaggingCreate) SetThread(t *Thread) *ThreadTaggingCreate {
	return ttc.SetThreadID(t.ID)
}

// SetTagID sets the "tag" edge to the ThreadTag entity by ID.
func (ttc *ThreadTaggingCreate) SetTagID(id int) *ThreadTaggingCreate {
	ttc.mutation.SetTagID(id)
	return ttc
}

// SetTag sets the "tag" edge to the ThreadTag entity.
func (ttc *ThreadTaggingCreate) SetTag(t *ThreadTag) *ThreadTaggingCreate {
	return ttc.SetTagID(t.ID)
}

// Mutation returns the ThreadTaggingMutation object of the builder.
func (ttc *ThreadTaggingCreate) Mutation() *ThreadTaggingMutation {
	return ttc.mutation
}

// Save creates the ThreadTagging in the database.
func (ttc *ThreadTaggingCreate) Save(ctx context.Context) (*ThreadTagging, error) {
	return withHooks(ctx, ttc.sqlSave, ttc.mutation, ttc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (ttc *ThreadTaggingCreate) SaveX(ctx context.Context) *ThreadTagging {
	v, err := ttc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (ttc *ThreadTaggingCreate) Exec(ctx context.Context) error {
	_, err := ttc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ttc *ThreadTaggingCreate) ExecX(ctx context.Context) {
	if err := ttc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (ttc *ThreadTaggingCreate) check() error {
	if _, ok := ttc.mutation.ThreadId(); !ok {
		return &ValidationError{Name: "threadId", err: errors.New(`ent: missing required field "ThreadTagging.threadId"`)}
	}
	if _, ok := ttc.mutation.TagId(); !ok {
		return &ValidationError{Name: "tagId", err: errors.New(`ent: missing required field "ThreadTagging.tagId"`)}
	}
	if _, ok := ttc.mutation.ThreadID(); !ok {
		return &ValidationError{Name: "thread", err: errors.New(`ent: missing required edge "ThreadTagging.thread"`)}
	}
	if _, ok := ttc.mutation.TagID(); !ok {
		return &ValidationError{Name: "tag", err: errors.New(`ent: missing required edge "ThreadTagging.tag"`)}
	}
	return nil
}

func (ttc *ThreadTaggingCreate) sqlSave(ctx context.Context) (*ThreadTagging, error) {
	if err := ttc.check(); err != nil {
		return nil, err
	}
	_node, _spec := ttc.createSpec()
	if err := sqlgraph.CreateNode(ctx, ttc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	return _node, nil
}

func (ttc *ThreadTaggingCreate) createSpec() (*ThreadTagging, *sqlgraph.CreateSpec) {
	var (
		_node = &ThreadTagging{config: ttc.config}
		_spec = sqlgraph.NewCreateSpec(threadtagging.Table, nil)
	)
	if nodes := ttc.mutation.ThreadIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: false,
			Table:   threadtagging.ThreadTable,
			Columns: []string{threadtagging.ThreadColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(thread.FieldID, field.TypeInt),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.ThreadId = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := ttc.mutation.TagIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: false,
			Table:   threadtagging.TagTable,
			Columns: []string{threadtagging.TagColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(threadtag.FieldID, field.TypeInt),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.TagId = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// ThreadTaggingCreateBulk is the builder for creating many ThreadTagging entities in bulk.
type ThreadTaggingCreateBulk struct {
	config
	err      error
	builders []*ThreadTaggingCreate
}

// Save creates the ThreadTagging entities in the database.
func (ttcb *ThreadTaggingCreateBulk) Save(ctx context.Context) ([]*ThreadTagging, error) {
	if ttcb.err != nil {
		return nil, ttcb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(ttcb.builders))
	nodes := make([]*ThreadTagging, len(ttcb.builders))
	mutators := make([]Mutator, len(ttcb.builders))
	for i := range ttcb.builders {
		func(i int, root context.Context) {
			builder := ttcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*ThreadTaggingMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, ttcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, ttcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, ttcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (ttcb *ThreadTaggingCreateBulk) SaveX(ctx context.Context) []*ThreadTagging {
	v, err := ttcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (ttcb *ThreadTaggingCreateBulk) Exec(ctx context.Context) error {
	_, err := ttcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ttcb *ThreadTaggingCreateBulk) ExecX(ctx context.Context) {
	if err := ttcb.Exec(ctx); err != nil {
		panic(err)
	}
}
