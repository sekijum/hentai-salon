// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"server/infrastructure/ent/forum"
	"server/infrastructure/ent/user"
	"server/infrastructure/ent/userforumsubscription"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// UserForumSubscription is the model entity for the UserForumSubscription schema.
type UserForumSubscription struct {
	config `json:"-"`
	// UserId holds the value of the "userId" field.
	UserId int `json:"userId,omitempty"`
	// ForumId holds the value of the "forumId" field.
	ForumId int `json:"forumId,omitempty"`
	// 通知を受け取るかどうかのカラムを追加
	IsNotified bool `json:"isNotified,omitempty"`
	// SubscribedAt holds the value of the "subscribedAt" field.
	SubscribedAt time.Time `json:"subscribedAt,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the UserForumSubscriptionQuery when eager-loading is set.
	Edges        UserForumSubscriptionEdges `json:"edges"`
	selectValues sql.SelectValues
}

// UserForumSubscriptionEdges holds the relations/edges for other nodes in the graph.
type UserForumSubscriptionEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// Forum holds the value of the forum edge.
	Forum *Forum `json:"forum,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserForumSubscriptionEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// ForumOrErr returns the Forum value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserForumSubscriptionEdges) ForumOrErr() (*Forum, error) {
	if e.Forum != nil {
		return e.Forum, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: forum.Label}
	}
	return nil, &NotLoadedError{edge: "forum"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UserForumSubscription) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case userforumsubscription.FieldIsNotified:
			values[i] = new(sql.NullBool)
		case userforumsubscription.FieldUserId, userforumsubscription.FieldForumId:
			values[i] = new(sql.NullInt64)
		case userforumsubscription.FieldSubscribedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UserForumSubscription fields.
func (ufs *UserForumSubscription) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case userforumsubscription.FieldUserId:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field userId", values[i])
			} else if value.Valid {
				ufs.UserId = int(value.Int64)
			}
		case userforumsubscription.FieldForumId:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field forumId", values[i])
			} else if value.Valid {
				ufs.ForumId = int(value.Int64)
			}
		case userforumsubscription.FieldIsNotified:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field isNotified", values[i])
			} else if value.Valid {
				ufs.IsNotified = value.Bool
			}
		case userforumsubscription.FieldSubscribedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field subscribedAt", values[i])
			} else if value.Valid {
				ufs.SubscribedAt = value.Time
			}
		default:
			ufs.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UserForumSubscription.
// This includes values selected through modifiers, order, etc.
func (ufs *UserForumSubscription) Value(name string) (ent.Value, error) {
	return ufs.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the UserForumSubscription entity.
func (ufs *UserForumSubscription) QueryUser() *UserQuery {
	return NewUserForumSubscriptionClient(ufs.config).QueryUser(ufs)
}

// QueryForum queries the "forum" edge of the UserForumSubscription entity.
func (ufs *UserForumSubscription) QueryForum() *ForumQuery {
	return NewUserForumSubscriptionClient(ufs.config).QueryForum(ufs)
}

// Update returns a builder for updating this UserForumSubscription.
// Note that you need to call UserForumSubscription.Unwrap() before calling this method if this UserForumSubscription
// was returned from a transaction, and the transaction was committed or rolled back.
func (ufs *UserForumSubscription) Update() *UserForumSubscriptionUpdateOne {
	return NewUserForumSubscriptionClient(ufs.config).UpdateOne(ufs)
}

// Unwrap unwraps the UserForumSubscription entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ufs *UserForumSubscription) Unwrap() *UserForumSubscription {
	_tx, ok := ufs.config.driver.(*txDriver)
	if !ok {
		panic("ent: UserForumSubscription is not a transactional entity")
	}
	ufs.config.driver = _tx.drv
	return ufs
}

// String implements the fmt.Stringer.
func (ufs *UserForumSubscription) String() string {
	var builder strings.Builder
	builder.WriteString("UserForumSubscription(")
	builder.WriteString("userId=")
	builder.WriteString(fmt.Sprintf("%v", ufs.UserId))
	builder.WriteString(", ")
	builder.WriteString("forumId=")
	builder.WriteString(fmt.Sprintf("%v", ufs.ForumId))
	builder.WriteString(", ")
	builder.WriteString("isNotified=")
	builder.WriteString(fmt.Sprintf("%v", ufs.IsNotified))
	builder.WriteString(", ")
	builder.WriteString("subscribedAt=")
	builder.WriteString(ufs.SubscribedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// UserForumSubscriptions is a parsable slice of UserForumSubscription.
type UserForumSubscriptions []*UserForumSubscription
